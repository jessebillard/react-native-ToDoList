export default (state = {notes: []}, action) => {
    switch (action.type) {
        case 'ADD_NOTE':
            return {

            }
        case 'REMOVE_NOTE':
            return {

            }
    }
}